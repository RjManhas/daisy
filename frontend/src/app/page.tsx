import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Daisy - Home',
};

export default function HomePage() {
  return <HomeView />;
}
